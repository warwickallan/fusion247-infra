# fusion247-infra — environment TEMPLATE (key names only, NO values).
# Real values live only in Coolify environment / protected local stores — never here.

# ── Unified / Capture Gateway (Telegram) ──
TELEGRAM_BOT_TOKEN=
AUTHORISED_TELEGRAM_USER_ID=
WORKER_ID=
DATABASE_URL=
DATABASE_SSL_CA_FILE=
CLICKUP_TOKEN=

# ── Directus ──
DIRECTUS_KEY=
DIRECTUS_SECRET=
DIRECTUS_DB_CONNECTION_STRING=
NO_UPDATE_NOTIFIER=1

# ── Redis ──
REDIS_PASSWORD=

# ── Neo4j ──
NEO4J_AUTH=
NEO4J_HEAP_MAX=
NEO4J_PAGECACHE=

# ── LightRAG ──
LIGHTRAG_API_KEY=
EMBEDDING_PROVIDER_API_KEY=
LIGHTRAG_MODEL=
LIGHTRAG_EMBEDDING_MODEL=

# ── Managed Honcho Cloud ──
HONCHO_API_KEY=
HONCHO_WORKSPACE=

# ── Coolify ──
COOLIFY_URL=
