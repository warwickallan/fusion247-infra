# ── Secrets: NEVER commit (repo is PUBLIC) ──────────────────────────
*.env
.env
.env.*
!.env.example
*.key
*.pem
*.crt
*.p12
*.pfx
id_ed25519*
id_rsa*
*secret*
*token*
*credential*
# Exception: this doc lists secret *locations* only (no values) and must be tracked
!docs/secrets-model.md
*.tfstate
*.tfstate.*
coolify*.secret
honcho*.key
supabase*.conn

# ── Environment / runtime ───────────────────────────────────────────
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
tmp/
*.log

# ── Backups / bundles (may contain data) ────────────────────────────
*.bundle
*.dump
recovery-*.enc
backups/*
!backups/.gitkeep

# ── OS ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
